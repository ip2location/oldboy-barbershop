import { HeaderPage } from '@/components/features/header';

export default function Home() {
  return (
    <main className="overflow-hidden flex min-h-screen flex-col bg-header-bg relative">
      <HeaderPage />
    </main>
  );
}
