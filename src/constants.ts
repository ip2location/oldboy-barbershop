export const SEVEN_DAYS = new Date(new Date().getTime() + 7 * 24 * 60 * 60 * 1000);

export enum Cookies {
  City = 'city',
  Language = 'language',
}
export enum Languages {
  EN = 'en',
  RU = 'ru',
}

export enum CityID {
  ID = 'cityID',
}
